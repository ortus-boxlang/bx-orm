/**
 * Fixture for the uniquekey/index mapping regression tests (TableConstraintsTest).
 */
class persistent="true" table="constrained_things" {

	property name="id" fieldtype="id" generator="increment" ormType="integer";

	property name="firstName" column="first_name" ormType="string" length="50" uniquekey="uk_ct_name";

	property name="lastName" column="last_name" ormType="string" length="50" uniquekey="uk_ct_name";

	property name="status" ormType="string" length="20" index="idx_ct_status";

	property name="code" ormType="string" length="20" notnull="true";

}
